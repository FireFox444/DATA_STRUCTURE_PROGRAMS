/*
write a program that will read n elements in one dimentional array.
Find out and print the total number of elements having value greater than 10
*/

#include<iostream.h>
#include<conio.h>

void main()
{
	cout<<"print the total number of elements having value greater than 10"<<endl;
	int arr[5];
	int count = 0;
	clrscr();

	cout<<"Enter array elements:"<<endl;
	for(int i = 0; i < 5; i++)
	{
		cin>>arr[i];
	}

	for(int j = 0; j < 5; j++)
	{
		if(arr[j] > 10)
		{
			count++;
		}
	}

	cout<<"Total number of elements having value greater than 10: "<<count<<endl;
	getch();
}